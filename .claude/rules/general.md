# Guidelines

* DO NOT use em dash '—' ANYWHERE, ALWAYS use standard dash '-'
* Branch names: use 'ab/{JIRA_TICKET_ID}' for branch names, resolve the jira ticket in the context if
existing, e.g. PLAN-1234
