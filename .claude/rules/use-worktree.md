# Worktrees

When working on any code changes inside a git tracked repo inside, ALWAYS working on a worktree.

## Conventions

* Naming: refer to General Guidelines for branch naming convention, align worktree name with branch name
